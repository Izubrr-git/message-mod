# Message Mod для Minecraft 1.21.8

Fabric мод, который позволяет игрокам отправлять сообщения на сервер через GUI, с последующим сохранением в PostgreSQL базу данных с использованием Protobuf для сериализации.

## 🚀 Возможности

- **GUI экран** с текстовым полем и кнопкой отправки (стандартное Minecraft API)
- **Protobuf 3** для эффективной сериализации данных
- **PostgreSQL** для надежного хранения сообщений
- **Hibernate + HikariCP** для управления подключением к БД
- **Официальные Mojang маппинги** через Yarn
- **Горячая клавиша** (M по умолчанию) для быстрого открытия GUI

## 📋 Требования

### Для сборки:
- Java 21 или выше
- Gradle 8.14
- Доступ к интернету (для загрузки зависимостей)

### Для запуска сервера:
- PostgreSQL 12 или выше
- База данных с именем `minecraft`

## 🛠️ Установка и настройка

### 1. Настройка PostgreSQL

Создайте базу данных и таблицу:

```sql
-- Создаем базу данных
CREATE DATABASE minecraft;

-- Подключаемся к ней
\c minecraft

-- Создаем таблицу (или позвольте Hibernate сделать это автоматически)
CREATE TABLE messages (
    id SERIAL PRIMARY KEY,
    uuid UUID NOT NULL,
    text VARCHAR(256) NOT NULL
);
```

### 2. Настройка подключения к БД

Отредактируйте файл `src/main/resources/hibernate.cfg.xml`:

```xml
<property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/minecraft</property>
<property name="hibernate.connection.username">YOUR_USERNAME</property>
<property name="hibernate.connection.password">YOUR_PASSWORD</property>
```

**Важно:** Замените `YOUR_USERNAME` и `YOUR_PASSWORD` на реальные данные!

### 3. Сборка мода

```bash
# Клонируйте или скопируйте проект
cd minecraft-message-mod

# Дайте права на выполнение gradlew (Linux/Mac)
chmod +x gradlew

# Соберите мод
./gradlew build

# Для Windows используйте:
gradlew.bat build
```

После успешной сборки файл мода будет находиться в:
```
build/libs/message-mod-1.0.0.jar
```

### 4. Установка мода

1. Установите [Fabric Loader](https://fabricmc.net/use/) для Minecraft 1.21.8
2. Скопируйте файл `.jar` в папку `mods` вашего клиента/сервера
3. Также установите [Fabric API](https://www.curseforge.com/minecraft/mc-mods/fabric-api)

## 🎮 Использование

### Для игрока:

1. Запустите игру
2. Нажмите клавишу **M** (или измените в настройках управления)
3. Введите текст сообщения (до 256 символов)
4. Нажмите **"Отправить"** или **Enter**
5. Сообщение будет отправлено на сервер и сохранено в БД

### Проверка в базе данных:

```sql
-- Посмотреть все сообщения
SELECT * FROM messages;

-- Посмотреть сообщения конкретного игрока
SELECT * FROM messages WHERE uuid = 'UUID_игрока';

-- Посмотреть последние 10 сообщений
SELECT * FROM messages ORDER BY id DESC LIMIT 10;
```

## 📁 Структура проекта

```
minecraft-message-mod/
├── build.gradle                    # Конфигурация Gradle
├── gradle.properties               # Версии зависимостей
├── settings.gradle                 # Настройки Gradle
├── src/
│   ├── main/
│   │   ├── java/com/example/messagemod/
│   │   │   ├── MessageMod.java                    # Серверная часть
│   │   │   ├── MessageModClient.java              # Клиентская часть
│   │   │   ├── client/gui/
│   │   │   │   └── MessageScreen.java             # GUI экран
│   │   │   ├── network/
│   │   │   │   └── NetworkHandler.java            # Сетевые пакеты
│   │   │   └── database/
│   │   │       ├── DatabaseManager.java           # Менеджер БД
│   │   │       ├── entity/
│   │   │       │   └── MessageEntity.java         # JPA Entity
│   │   │       └── repository/
│   │   │           └── MessageRepository.java     # Репозиторий
│   │   ├── proto/
│   │   │   └── message.proto                      # Protobuf схема
│   │   └── resources/
│   │       ├── fabric.mod.json                    # Метаданные мода
│   │       ├── hibernate.cfg.xml                  # Конфигурация Hibernate
│   │       └── assets/messagemod/lang/
│   │           ├── en_us.json                     # Английская локализация
│   │           └── ru_ru.json                     # Русская локализация
│   └── generated/                                 # Сгенерированные Protobuf классы
```

## 🔧 Разработка

### Генерация Protobuf классов

Protobuf классы генерируются автоматически при сборке. Для ручной генерации:

```bash
./gradlew generateProto
```

### Запуск тестового клиента/сервера

```bash
# Запуск клиента
./gradlew runClient

# Запуск сервера
./gradlew runServer
```

### Очистка сборки

```bash
./gradlew clean
```

## 🔍 Технические детали

### Protobuf сообщение

```protobuf
syntax = "proto3";

message Message {
  string text = 1;
}
```

### Схема БД

```sql
CREATE TABLE messages (
    id SERIAL PRIMARY KEY,        -- Автоинкрементный ID
    uuid UUID NOT NULL,           -- UUID игрока
    text VARCHAR(256) NOT NULL    -- Текст сообщения
);
```

### Hibernate преимущества

✅ **Автоматическое управление схемой** - Hibernate создаст таблицу автоматически
✅ **Пулинг соединений** - HikariCP оптимизирует работу с БД
✅ **Транзакции** - Автоматическая обработка транзакций
✅ **ORM** - Объектно-реляционное отображение упрощает работу с БД
✅ **Независимость от БД** - Легко сменить на MySQL/MariaDB/Oracle

### Альтернатива JPA Repository

Если хочешь использовать Spring Data JPA Repository pattern, нужно:

1. Добавить Spring Data JPA зависимость
2. Создать интерфейс репозитория:

```java
public interface MessageJpaRepository extends JpaRepository<MessageEntity, Long> {
    List<MessageEntity> findByUuid(UUID uuid);
    List<MessageEntity> findTop10ByOrderByIdDesc();
}
```

## 🐛 Решение проблем

### Мод не загружается

- Убедитесь, что установлен Fabric API
- Проверьте версию Java (должна быть 21+)
- Проверьте логи в `.minecraft/logs/latest.log`

### Ошибка подключения к БД

- Убедитесь, что PostgreSQL запущен
- Проверьте данные подключения в `hibernate.cfg.xml`
- Проверьте, что база данных `minecraft` создана

### Protobuf классы не генерируются

```bash
# Принудительная генерация
./gradlew clean generateProto
```